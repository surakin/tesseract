# yaml-language-server: $schema=https://aka.ms/winget-manifest.version.1.12.0.schema.json
# Generated by CMake from Tesseract.Matrix.yaml.in — edit that file, not
# this one. This is the version manifest — see
# Tesseract.Matrix.installer.yaml and Tesseract.Matrix.locale.en-US.yaml
# for the other two required files.
#
# Reference copy only — the published copy lives in
# manifests/t/Tesseract/Matrix/<version>/ in microsoft/winget-pkgs.
# See README.md for the submission/update procedure.
PackageIdentifier: Tesseract.Matrix
PackageVersion: @PROJECT_VERSION@
DefaultLocale: en-US
ManifestType: version
ManifestVersion: 1.12.0
